# yaml-language-server: $schema=../_global/ibek.defs.schema.json

module: dls_psc_support

defs:
  - name: Psc
    args:
      - name: carrier
        type: object
        description: IPAC carrier name

    script:
      - pscAddIpModule(IPACid, ipslot, vector, link)
