# yaml-language-server: $schema=http://github.com/epics-containers/ibek/releases/download/0.9/ibek.defs.schema.json

module: psc

defs:
  - name: PSC
    args:
      - name: carrier
        type: object
        description: IPAC carrier name

    script:
      - pscAddIpModule(IPACid, ipslot, vector, link)
