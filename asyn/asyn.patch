#!/usr/bin/env bash

# patch asyn for compatibility with the container base OS

if [[ $TARGET_ARCHITECTURE != "rtems" ]]; then
    # Enable TIRPC for ASYN
    sed -i 's:# TIRPC:TIRPC:g' ${SUPPORT}/asyn/configure/CONFIG_SITE
fi
sed -i 's:IPAC=:# IPAC=:g' ${SUPPORT}/asyn/configure/RELEASE

