label: NDPluginAttribute
parent: NDPluginDriver
children:

- type: Group
  name: NDAttribute
  layout:
    type: Grid
    labelled: true
  children:

  - type: SignalW
    name: Reset
    write_pv: $(P)$(R)Reset
    write_widget:
      type: ToggleButton
