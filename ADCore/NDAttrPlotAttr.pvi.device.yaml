label: NDAttrPlotAttr
parent: NDPluginBase
children:

- type: Group
  name: ADAttrFile
  layout:
    type: Grid
    labelled: true
  children:

  - type: SignalR
    name: Attr
    read_pv: $(P)$(R)Attr$(ATTR_IND)
    read_widget:
      type: TextRead
