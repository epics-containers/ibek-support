label: NDPvxs
parent: NDPluginBase
children:

- type: Group
  name: NDPvxs
  layout:
    type: Grid
    labelled: true
  children:

  - type: SignalR
    name: PvName
    read_pv: $(P)$(R)PvName_RBV
    read_widget:
      type: TextRead
      format: string
