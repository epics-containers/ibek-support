# yaml-language-server: $schema=../_scripts/support_install_variables.json

module: pvaDriver
version: fee9e5a240eb4209cd01e24cfc6d5273159a39de
dbds:
  - pvaDriverSupport.dbd
libs:
  - pvaDriver

organization: http://github.com/areaDetector/

patch_blocks:
  - path: "{{ config_linux_host }}"
    marker: "# {mark} disable PVAcpp (using PVXS instead) -----------------------"
    block: |
      WITH_PVA = NO
